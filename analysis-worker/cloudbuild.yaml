steps:
  # Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/geowatch-analysis-worker:$SHORT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/geowatch-analysis-worker:latest'
      - '-f'
      - 'analysis-worker/Dockerfile'
      - '.'

  # Push to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/geowatch-analysis-worker:$SHORT_SHA'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/geowatch-analysis-worker:latest'

images:
  - 'gcr.io/$PROJECT_ID/geowatch-analysis-worker:$SHORT_SHA'
  - 'gcr.io/$PROJECT_ID/geowatch-analysis-worker:latest'

timeout: '3600s'
